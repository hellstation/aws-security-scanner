#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

if [ -x "$ROOT_DIR/venv/bin/python" ]; then
  exec "$ROOT_DIR/venv/bin/python" -m awscan.main "$@"
fi

exec python3 -m awscan.main "$@"
